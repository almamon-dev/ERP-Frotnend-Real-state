import React from 'react';

export interface AlertDialogProps {
    className?: string;
}

export default function AlertDialog({ className }: AlertDialogProps) {
    return (
        <div className={className}>
            {/* AlertDialog Component */}
        </div>
    );
}
