import React from 'react';

export interface FormLabelProps {
    className?: string;
}

export default function FormLabel({ className }: FormLabelProps) {
    return (
        <div className={className}>
            {/* FormLabel Component */}
        </div>
    );
}
