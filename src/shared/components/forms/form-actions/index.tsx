import React from 'react';

export interface FormActionsProps {
    className?: string;
}

export default function FormActions({ className }: FormActionsProps) {
    return (
        <div className={className}>
            {/* FormActions Component */}
        </div>
    );
}
