import React from 'react';

export interface ConfirmDialogProps {
    className?: string;
}

export default function ConfirmDialog({ className }: ConfirmDialogProps) {
    return (
        <div className={className}>
            {/* ConfirmDialog Component */}
        </div>
    );
}
